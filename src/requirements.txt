# App-only dependencies for Streamlit Community Cloud deployment.
# See runtime.txt for the Python version pin (3.12).
#
# scikit-learn must stay exact — joblib artifacts are version-sensitive.
# Everything else uses >= so uv/pip can grab pre-built 3.12 wheels directly.

scikit-learn==1.6.1
joblib==1.4.2
pandas>=2.2.3
numpy>=1.26.0,<3
shap>=0.51.0
matplotlib>=3.9.0
seaborn>=0.13.2
streamlit>=1.35.0
pillow>=10.3.0
