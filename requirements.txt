# Pinned to the versions used when training the artifacts under outputs/models/.
# joblib model artifacts are sensitive to scikit-learn major versions, so the
# core runtime pins are exact. TabPFN is constrained separately below.
pandas==2.2.3
numpy==2.2.6
scikit-learn==1.6.1
matplotlib==3.10.1
seaborn==0.13.2
openpyxl==3.1.5
joblib==1.4.2
shap==0.51.0
streamlit==1.32.0
jupyter==1.0.0
nbconvert==7.17.0
reportlab==4.4.9
pillow==10.3.0
pypdf==4.0.0
pymupdf==1.24.0
# TabPFN-v2 (Hollmann et al., Nature 2025) - pinned <7 to use open-weights
# checkpoint that does not require the browser-based license token.
tabpfn<7
